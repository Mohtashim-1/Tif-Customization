# Training Schedule Vue portal
#
# Dev:
#   cd apps/tif_customization/training_schedule && yarn && yarn dev
#
# Production build (outputs to tif_customization/public/training_schedule):
#   cd apps/tif_customization/training_schedule && yarn && yarn build
#   bench build --app tif_customization   # optional, links assets
#   bench clear-cache
#
# Open: /training-schedule
